# Xlang
Interpretador XLang para HTML - Tags personalizadas que adicionam comportamento dinâmico ao HTML. Importe via CDN e use tags como &lt;var>, &lt;if>, &lt;loop>, &lt;print> sem necessidade de build ou ferramentas adicionais.
